# Environment Configuration
# This app runs entirely in the browser - no backend required!

# For local development, no environment variables needed
# The app uses browser localStorage for data storage

# If you want to use a custom backend, uncomment and set the URL:
# VITE_API_URL=http://localhost:3001/api